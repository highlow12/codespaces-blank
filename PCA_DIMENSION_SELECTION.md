# PCA 차원 선택 기록

이 문서는 AG News 768차원 임베딩에서 클러스터링과 UMAP 시각화에 사용할
PCA 차원을 선택한 과정과 결과를 보존한다. 분석 worktree는 이후 제거하지만,
아래의 기준과 수치는 main에 남긴다.

## 결론

- 클러스터링: **PCA-256**
- UMAP 시각화: **PCA-64 -> UMAP-2**
- UMAP soft target: 정규화된 soft cluster membership 4차원, `target_weight=0.01`

클러스터링은 원본 공간의 정보와 이웃 구조를 더 많이 유지하도록 256차원을
사용한다. UMAP은 2차원으로 다시 압축되므로, 64차원에서 속도와 구조 보존의
균형을 잡는다.

## 평가 방법

모든 비교에서 다음 조건을 고정했다.

- 입력: 4,000개, 원본 임베딩 768차원
- PCA 입력: L2 정규화된 원본 임베딩
- UMAP: `n_components=2`, `n_neighbors=15`, `min_dist=0.02`,
  `metric=cosine`, `spread=0.85`, `densmap=False`
- UMAP seed: 42 (차원 간 직접 비교)
- 이웃 비교: 각 점의 k-NN 집합에 대해 `|교집합| / k`와 Jaccard 계산
- k 값: 15, 30

PCA-16/32/64의 초기 선택에서는 seed 11, 42, 73을 사용해 UMAP을 반복하고,
로컬 이웃 보존율·전역 거리 순위 상관·seed 간 이웃 그래프 안정성을 측정했다.
최고 평균의 1표준오차 안에 드는 가장 작은 차원을 선택하는 규칙을 사용했다.

## 누적 설명 분산

정규화된 768차원 임베딩에 full-SVD PCA를 적용한 값이다.

| PCA 차원 | 누적 설명 분산 |
|---:|---:|
| 16 | 31.87% |
| 32 | 45.30% |
| 64 | 61.57% |
| 128 | 79.09% |
| 256 | 92.96% |

## 원본과 PCA 공간의 최근접 이웃

`intersection/k` 기준이다. PCA 차원이 커질수록 원본 이웃 보존율이
일관되게 증가한다.

| 차원 | k=15 | k=30 |
|---:|---:|---:|
| PCA-16 | 36.51% | 43.32% |
| PCA-32 | 53.12% | 59.23% |
| PCA-64 | 68.14% | 72.42% |
| PCA-128 | 80.82% | 82.72% |
| PCA-256 | 87.17% | 87.82% |

## 원본과 UMAP-2의 최근접 이웃

UMAP으로 2차원까지 압축하면 차원 증가 효과가 크게 줄어든다. 그래도
PCA-16에서 64로 올릴 때는 개선이 분명하고, 128/256은 64보다 소폭만
개선된다.

| UMAP 입력 PCA | k=15 | k=30 |
|---:|---:|---:|
| PCA-16 | 21.59% | 27.72% |
| PCA-32 | 28.59% | 34.66% |
| PCA-64 | 31.20% | 36.85% |
| PCA-128 | 32.32% | **37.13%** |
| PCA-256 | **32.36%** | 37.04% |

PCA-128/256의 UMAP 결과가 PCA-64보다 약간 높지만, PCA-256의 추가 비용에
비해 UMAP-2의 이웃 보존 이득은 작다. 따라서 시각화 기본값은 PCA-64로
정했다. PCA-16과 32는 이웃 구조 손실이 더 커서 기본값으로 선택하지 않았다.

## UMAP 결과끼리의 이웃 비교

각 차원으로 생성한 UMAP-2 좌표의 k-NN 집합을 직접 비교한 결과다.

| 비교 | k=15 | k=30 |
|---|---:|---:|
| UMAP-16 vs UMAP-32 | 28.36% | 37.15% |
| UMAP-16 vs UMAP-64 | 22.25% | 29.75% |
| UMAP-32 vs UMAP-64 | 37.19% | 47.59% |

UMAP-16은 UMAP-64와 특히 다르다. UMAP-32와 64도 완전히 같은 결과는
아니지만, 16보다는 서로 가깝다.

## 해석과 한계

설명 분산만으로 클러스터링 차원을 결정하지 않았다. PCA-256은 현재 데이터에서
92.96%의 분산과 가장 높은 원본 이웃 보존율을 가지므로 클러스터링용 보수적
기본값으로 채택했다. 실제 운영 데이터나 임베딩 모델이 바뀌면 클러스터
안정성, 원본 공간 silhouette, 필요시 ARI/NMI를 다시 측정해야 한다.

시각화 차원 선택은 UMAP의 성격에 맞춰 로컬 이웃 보존율을 1차 기준으로 삼았다.
UMAP은 매번 재학습하지 않고, 선택한 PCA와 UMAP 모델을 저장해 온라인에서는
transform만 수행해야 한다.

분석 provenance:

- 분석 worktree branch: `feature/pca-dimension-selection`
- 마지막 분석 commit: `5f4a1f2`
- main에 반영된 설정 commit: `f10bbc3`
