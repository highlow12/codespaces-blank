[package]
name = "atomic-clusters-wasm-core"
version = "0.1.0"
edition = "2021"
description = "Numerical kernels for Atomic Clusters"
license = "MIT"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
serde = { version = "1", features = ["derive"] }
serde-wasm-bindgen = "0.6"
