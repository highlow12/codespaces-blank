/**
 * Replaced by build.mjs with the checked-in pyodide_core package. Keeping an
 * empty source map here makes the TypeScript source independently type-check;
 * release bundles always embed the package and never fetch Python source.
 */
export const PYODIDE_CORE_SOURCE: Record<string, string> = {};
