# Gemini 태그 임베딩 클러스터링 실험

## 결론

내용 임베딩에 정규화된 태그 임베딩을 추가하면 클러스터 품질이 크게
개선됐다. 다만 이번 태그는 DBpedia의 정답 `class`에서 생성했으므로,
실제 운영 태그의 성능이 아니라 태그 신호의 상한에 가까운 입력 수준
검증이다.

- 태그 노이즈 분산이 차원별 `1e-4`까지는 태그 의미와 품질 개선이 유지됐다.
- 차원별 `1e-3`에서는 태그 방향이 크게 붕괴하고 내용만 사용하는 경우에
  가까워졌다.
- 분산 `1e-4`에서 태그 가중치 `1.5~2.0`가 안정적으로 좋은 구간이었다.
  가중치 `3.0`이 1000개 표본에서 가장 높았지만 태그 블록의 영향력이 약
  90%이므로, 초기 운영값으로는 `2.0`이 더 보수적이다.

## 실험 설정

- 내용: 새로 생성한 Gemini 문서 임베딩 `dbpedia_gemini_embeddings.json`
- 태그: Gemini 클래스 라벨 임베딩 `dbpedia_label_embeddings.json`
- 입력 구성:

  ```text
  L2-normalize(content) || tag_weight * L2-normalize(tag)
  ```

- 기존 auto PCA와 FCM 자동 군집 수 선택을 사용했다. 군집 수 선택 범위는
  `2~24`, 최소 자식 크기는 `20`, 선택 방식은 `multi_metric`이다.
- PCA, 군집 수 선택, FCM에는 정답 라벨을 전달하지 않았다. NMI/ARI와
  클래스별 색상은 클러스터링 이후 평가·시각화에만 사용했다.
- 태그 벡터에 차원별 Gaussian noise `N(0, variance)`를 추가한 뒤 문서별
  L2 정규화를 다시 적용했다.

## 1. 노이즈 분산 sweep — 태그 가중치 1.0

| 조건 | 분산 | 자동 k | NMI | ARI | Silhouette | XB | 수정 PC | 정규화 PE |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 내용만 | - | 2 | 0.222 | 0.067 | 0.044 | 0.239 | 0.058 | 0.957 |
| 태그, 무노이즈 | 0 | 10 | 0.786 | 0.536 | 0.243 | 0.088 | 0.100 | 0.888 |
| 태그 | `1e-5` | 10 | 0.781 | 0.533 | 0.238 | 0.088 | 0.097 | 0.890 |
| 태그 | `1e-4` | 10 | 0.767 | 0.522 | 0.211 | 0.092 | 0.076 | 0.911 |
| 태그 | `1e-3` | 2 | 0.271 | 0.082 | 0.044 | 0.240 | 0.056 | 0.959 |

`1e-7`과 `1e-6`은 무노이즈 조건과 거의 같았다. XB와 정규화 PE는 낮을수록,
Silhouette와 수정 PC는 높을수록 좋다. 원시 PC/PE는 자동으로 선택된 k에
따라 값이 달라지므로 수정 PC와 정규화 PE를 함께 확인했다.

## 2. 태그 가중치 sweep — 분산 `1e-4`

전체 데이터 sweep이 오래 걸려 동일한 시드로 중복 없이 1000개를 추출해
비교했다. 표본 결과는 다음과 같다.

| 태그 가중치 | 자동 k | NMI | ARI | Silhouette | 수정 PC | 정규화 PE |
|---:|---:|---:|---:|---:|---:|---:|
| 내용만 | 2 | 0.216 | 0.067 | 0.049 | 0.067 | 0.950 |
| 0.10 | 2 | 0.219 | 0.067 | 0.049 | 0.068 | 0.950 |
| 0.25 | 2 | 0.224 | 0.068 | 0.049 | 0.068 | 0.949 |
| 0.50 | 5 | 0.512 | 0.260 | 0.086 | 0.063 | 0.940 |
| 0.75 | 5 | 0.545 | 0.269 | 0.087 | 0.074 | 0.929 |
| 1.00 | 7 | 0.655 | 0.400 | 0.152 | 0.104 | 0.894 |
| 1.50 | 8 | 0.740 | 0.463 | 0.225 | 0.152 | 0.846 |
| 2.00 | 8 | 0.759 | 0.479 | 0.252 | 0.196 | 0.807 |
| 3.00 | 8 | 0.760 | 0.482 | 0.269 | 0.238 | 0.769 |

가중치 `w`가 만드는 태그 블록의 사전 정규화 에너지 비율은
`w² / (1+w²)`이다. 따라서 `w=1.5`는 약 69%, `w=2.0`은 80%를 태그
블록이 차지한다. `1.5`와 `2.0`은 모두 k=8을 선택했고, `2.0`에서 군집이
조금 더 조밀하고 분리되는 시각적 경향과 지표 개선이 함께 나타났다.
다만 UMAP 좌표는 조건별로 독립적으로 적합했으므로 시각화는 정성적
비교로만 해석한다.

## 태그 결합 코드 사용법

재사용 코드는 [`tag_embedding_features.py`](tag_embedding_features.py)에
있다. 문서와 태그를 class 기준으로 정렬하고, 태그 임베딩을 정규화한 뒤
기존 비지도 파이프라인에 전달할 feature matrix를 만든다.

```python
from pathlib import Path

from tag_embedding_features import (
    build_tag_augmented_features,
    load_dbpedia_embedding_pair,
)

content, metadata, tags, hierarchies = load_dbpedia_embedding_pair(
    Path("dbpedia_gemini_embeddings.json"),
    Path("dbpedia_label_embeddings.json"),
)
features = build_tag_augmented_features(
    content,
    tags,
    tag_weight=2.0,
)

# features를 기존 비지도 PCA/FCM 파이프라인에 전달한다.
# metadata["class"] 같은 정답 라벨은 클러스터링 호출에 전달하지 않는다.
```

실제 운영 검증에서는 정답 class에서 만든 태그 대신 독립적으로 수집된
태그를 사용하고, 누락 태그·오분류 태그·구조적 태그 오류를 별도 noise
모델로 평가해야 한다.
